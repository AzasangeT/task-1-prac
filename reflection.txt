The purpose of a shell is to take commands from the user and tells the operating system what to do. 2. By running shell commands, I learnt how to identify the type of shell commands and use them in a Bash script. 3. Using the terminal was different from clicking through folders because it gave me more control as i was using specific commands
